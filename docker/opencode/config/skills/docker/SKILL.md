---
name: docker
description: Use when troubleshooting Thor Docker containers with docker ps, docker logs, or docker stats.
---

# Docker troubleshooting

Use the normal Docker CLI spelling for supported troubleshooting:

- `docker ps ...`
- `docker logs ...`
- `docker stats ...`

Unsupported flags or subcommands are rejected by server-side policy; treat that denial as authoritative.
